# California Martial Athletics Static Site

Static Cloudflare Pages build generated from the `www_cmasf_com*.json` capture files in this repository.

## Build

```bash
npm run build
```

Cloudflare Pages should use:

- Build command: `npm run build`
- Output directory: `dist`
- Production branch: `main`
